# multimodal-content-analysis-agent
Multi-modal, function-calling AI agent for airline customer support that ingests text, audio, and images, runs intent and sentiment analysis, calls tools (SQLite example), and generates a 1-sentence response with a lightweight Gradio UI.
